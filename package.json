{
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun test",
    "download": "bun run ./scripts/download/index.ts",
    "generate": "bun run ./scripts/generate/index.ts"
  },
  "devDependencies": {
    "@types/bun": "1"
  }
}