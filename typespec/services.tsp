model ProviderService {
    // The Graph network ID
    id: string;

    // Type of service
    type: string;

    // URL endpoint for the service
    url: string;
}

model Service {
    // ID of the service
    id: string;

    // URL endpoint for the service
    url: string;
}
